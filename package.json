{
  "name": "project-js",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "homepage": "https://dimainit.github.io/spec-tranc-site/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.98.0",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "aos": "^2.3.4",
    "page": "^1.11.6"
  }
}
